<Window x:Class="WpfApp5.Views.ContractDetailsWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="📊 合約詳細資訊" Height="600" Width="900"
      WindowStartupLocation="CenterOwner"
      ResizeMode="CanResize">

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="5,2"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 標題區 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="📊 合約詳細資訊" Style="{StaticResource HeaderTextStyle}" FontSize="18"/>
            <TextBlock Name="ContractCountTextBlock" Text="(共 0 個合約)" 
                     VerticalAlignment="Center" Margin="10,0,0,0" 
                     FontWeight="Bold" Foreground="#7F8C8D"/>
        </StackPanel>

        <!-- 主要內容區 -->
        <TabControl Grid.Row="1" Name="ContractTabControl">
            <!-- 動態生成的 TabItem 會在這裡 -->
        </TabControl>

        <!-- 按鈕區 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="ExportButton" Content="📄 匯出資料" Style="{StaticResource ButtonStyle}" 
                  Click="ExportButton_Click" Background="#27AE60"/>
            <Button Name="CopyAllButton" Content="📋 複製全部" Style="{StaticResource ButtonStyle}" 
                  Click="CopyAllButton_Click" Background="#E67E22"/>
            <Button Name="CloseButton" Content="❌ 關閉" Style="{StaticResource ButtonStyle}" 
                  Click="CloseButton_Click" Background="#95A5A6"/>
        </StackPanel>
    </Grid>
</Window>